{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "スクリプトの実行: dev",
            "request": "launch",
            "command": "pnpm run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}